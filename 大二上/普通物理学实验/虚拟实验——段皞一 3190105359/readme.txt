1.既可以测交流，也可以测直流——
   拨动“交流直流选择档位”，可以切换测量交流和直流电压。
2.直流电压的量程选择——
   调节“直流信号量程选择”按钮，整数1-4分别代表10V、5V、3V、1V量程，
   其它数字（包括0在内）则为调零功能，即所有的直流信号显示表都显示0，
   但是直流电压仍然显示当前直流信号示数。
3.清零功能——
   a.测量交流直流时候，同一时间只显示一个示数。
   b.点击“stop”关闭万用表时，所有仪表都显示零（包括示波器）。
   c.切换直流信号的量程时，其余三个量程都显示零。